package solutions

import "testing"

func TestMyQueue(t *testing.T) {
	type TestCase struct {
		Name string
		//...
	}

	tcs := []TestCase{}

	for _, tc := range tcs {
		t.Run(tc.Name, func(t *testing.T) {

		})
	}
}
